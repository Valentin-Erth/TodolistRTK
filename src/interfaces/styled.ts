export interface ITheme {
  colors: {
    primary: string
    secondary: string
    warning: string
    success: string
    danger: string

    bg: {
      primary: string
      secondary: string
      active: string
    }
    font: {
      primary: string
      secondary: string
    }
  }
}